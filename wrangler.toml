name = "r2-dual-uploader"
compatibility_date = "2024-12-01"
compatibility_flags = ["nodejs_compat"]

[vars]
# Non-secret variables can go here, but secrets should be set via dashboard

# Pages will auto-detect the public directory
# No need for [site] config when using Pages
